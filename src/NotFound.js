import React from "react";

function NotFound() {
  //displays on any page that don't have a route. just deisplays not found.
  return (
    <div className="NotFound">
      <h1>Not Found</h1>
    </div>
  );
}

export default NotFound;